// Monorepo stub — expo/AppEntry.js is hoisted to <root>/node_modules/expo/
// and does `import App from '../../App'`, which resolves here.
// We forward to the actual mobile app entry point.
export { default } from './apps/mobile/App';
